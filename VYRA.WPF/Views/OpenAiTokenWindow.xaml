<Window x:Class="VYRA.WPF.Views.OpenAiTokenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenAI token"
        Width="460"
        Height="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Topmost="True"
        Background="#111111">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Введите OpenAI API token"
                   Foreground="#F2FFF0"
                   FontFamily="Consolas"
                   FontSize="15"
                   Margin="0,0,0,10"/>

        <PasswordBox x:Name="TokenBox"
                     Grid.Row="1"
                     Background="#151A17"
                     Foreground="#F2FFF0"
                     BorderBrush="#7ED957"
                     BorderThickness="1"
                     FontFamily="Consolas"
                     FontSize="14"
                     Padding="8"
                     PasswordChar="●"
                     Margin="0,0,0,8"
                     KeyDown="TokenBox_KeyDown"/>

        <TextBlock Grid.Row="2"
                   Text="Хранится локально через Windows DPAPI. Маленький сейф, не свалка."
                   Foreground="#B7C9B2"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Clear"
                    Width="78"
                    Margin="0,0,8,0"
                    Click="Clear_Click"/>
            <Button Content="Cancel"
                    Width="78"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Save"
                    Width="78"
                    IsDefault="True"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
